'use strict';

function calculate_elements_sum(collection, element) {
  //在这里写入代码
  return collection.indexOf(element) ;
}

module.exports = calculate_elements_sum;
// var collection = [1,11,27,20,4,9,15,4,1,11];
// console.log(collection.indexOf(4));
